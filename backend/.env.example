# Server
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Database
DB_NAME=dfet
DB_USER=dfet
DB_PASS=dfet123
DB_HOST=localhost
DB_PORT=3306

# Auth
JWT_SECRET=supersecretjwt
JWT_EXPIRES_IN=7d

# Spoonacular (optional)
SPOONACULAR_API_KEY=

# Web Push (VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# Email (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="DFET <no-reply@example.com>"
